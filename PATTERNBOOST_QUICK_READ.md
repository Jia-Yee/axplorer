# 🎯 PatternBoost 核心思想 - 5 分钟速读

## 一句话总结

**PatternBoost = 局部搜索 (保证有效性) + 全局搜索 (发现新模式) × 迭代增强**

---

## 为什么需要两种搜索？

### 问题：单一方法有致命缺陷

#### ❌ 只用局部搜索（贪心算法）

```
结果：陷入局部最优，找不到全局最优

例子：Turan 问题 (N=25)
- 局部搜索最佳：54-56 条边
- 理论最优：63 条边
- 差距：~15% 性能损失

原因：
1. 视野狭窄（只看邻域）
2. 路径依赖（初始解决定上限）
3. 缺乏创新（无法突破思维定式）
```

#### ❌ 只用全局搜索（神经网络）

```
结果：质量不稳定，产生无效解

问题：
1. Transformer 生成的图可能包含 4-环（违反约束）
2. 训练初期数据质量差，学到错误模式
3. 需要大量后处理修复
4. 黑盒操作，不可解释

原因：
1. 概率生成，不保证有效性
2. 缺乏领域知识
3. 试错成本高
```

---

### 解决方案：优势互补

| 能力 | 局部搜索 | 全局搜索 | PatternBoost 选择 |
|------|----------|----------|------------------|
| **保证有效性** | ✅✅ 擅长 | ❌ 困难 | 用局部搜索 |
| **发现新模式** | ❌ 困难 | ✅✅ 擅长 | 用全局搜索 |
| **快速收敛** | ✅✅ 高效 | ❌ 缓慢 | 用局部搜索 |
| **跳出局部最优** | ❌ 不能 | ✅✅ 能 | 用全局搜索 |
| **泛化能力** | ❌ 弱 | ✅✅ 强 | 用全局搜索 |

**结论**: 让每种方法做最擅长的事！

---

## 工作原理（图解）

### 迭代增强循环

```
┌─────────────────────────────────────────────────────┐
│          第 1 轮：从空白开始                         │
├─────────────────────────────────────────────────────┤
│ 局部搜索                                            │
│ • 从空图开始                                        │
│ • 贪心加边 → 得到一批中等解（平均 54 条边）           │
│                                                     │
│ ↓                                                   │
│                                                     │
│ 全局搜索                                            │
│ • 训练 Transformer 学习前 1% 的解                    │
│ • 发现模式："高质量图倾向于..."                     │
│ • 采样新种子（带有 learned pattern）                │
└─────────────────────────────────────────────────────┘
         ↓
┌─────────────────────────────────────────────────────┐
│          第 2 轮：站在巨人肩膀上                     │
├─────────────────────────────────────────────────────┤
│ 局部搜索（升级！）                                  │
│ • 用 Transformer 种子作为起点                       │
│ • 贪心加边 → 得到更好的解（平均 58 条边）           │
│                                                     │
│ ↓                                                   │
│                                                     │
│ 全局搜索（升级！）                                  │
│ • 用更优数据重新训练                                │
│ • refined 模式识别                                   │
│ • 采样更高质量的种子                                │
└─────────────────────────────────────────────────────┘
         ↓
┌─────────────────────────────────────────────────────┐
│          第 3-N 轮：持续改进                         │
│  ... → 最终达到 63 条边（最优解）!                   │
└─────────────────────────────────────────────────────┘
```

**关键洞察**:
- ✅ 每一轮都站在上一轮的肩膀上
- ✅ 形成正反馈循环
- ✅ 性能持续提升

---

## 实际效果对比

### Turan 问题 (N=25)

| 方法 | 最佳边数 | 时间 | 达到最优？ |
|------|----------|------|------------|
| 纯贪心 | 54-56 | 5 分钟 | ❌ |
| 模拟退火 | 58-60 | 2 小时 | ❌ |
| 专家手工 | 61-62 | 数周 | ❌ |
| **PatternBoost** | **63** | **15 分钟** | **✅** |

### 等腰三角形问题

| N | 已知最优 | PatternBoost | 提升 |
|---|----------|--------------|------|
| 20 | 27 | 29 | **+2** ⭐⭐ |
| 25 | 31 | 34 | **+3** ⭐⭐⭐ |

**打破思维定式**: 发现不对称但更高效的排列

---

## 哲学启示

### 与人类智慧的相似性

#### 科学家研究
```
局部搜索 = 实验试错（基于现有理论逐步改进）
全局搜索 = 理论创新（从成功案例提炼新模式）
迭代 = 新理论指导新实验，实验修正理论
```

#### 艺术家创作
```
局部搜索 = 技法磨练（反复练习）
全局搜索 = 灵感迸发（向大师学习）
迭代 = 新技术实现新风格，新风格催生新技术
```

#### 工程师设计
```
局部搜索 = 渐进优化（现有方案改进）
全局搜索 = 范式转移（引入全新思路）
迭代 = 新设计需要新技术，新技术支持新设计
```

### 深层原理

**双系统理论** (Kahneman):
- 系统 1（快思考）→ 局部搜索
- 系统 2（慢思考）→ 全局搜索
- **PatternBoost = 系统 1 + 系统 2**

**进化论**:
- 突变（探索）→ 全局搜索
- 自然选择（利用）→ 局部搜索
- **PatternBoost = 人工进化系统**

**辩证法**:
- 量变到质变：局部积累 → 全局飞跃
- 否定之否定：螺旋式上升
- **PatternBoost = 辩证法在 AI 中的体现**

---

## 技术要点

### 局部搜索做什么？

```python
def local_search(graph):
    """
    职责：保证有效性 + 贪心优化
    """
    # 1. 修复违规（如果有）
    while has_4_cycle(graph):
        remove_worst_edge(graph)
    
    # 2. 贪心加边
    while can_add_valid_edge(graph):
        add_random_valid_edge(graph)
    
    # 保证：输出一定合法且分数 >= 输入
    assert is_valid(graph)
    assert graph.score >= initial_score
    
    return graph
```

**特点**:
- ✅ 确定性规则（硬编码约束）
- ✅ 单调不减（保证改进）
- ✅ 高效（O(N³)）

---

### 全局搜索做什么？

```python
def global_search(best_solutions):
    """
    职责：学习模式 + 创造新种子
    """
    # 1. 训练 Transformer（只使用前 1% 最优解）
    model = Transformer()
    train(model, elite_data=top_1_percent(best_solutions))
    
    # 2. 采样新种子
    new_seeds = []
    for _ in range(pop_size):
        seed = model.sample(temperature=0.6, top_k=50)
        new_seeds.append(seed)
    
    return new_seeds
```

**学到的模式**:
```
"如果已经有边 (0,1) 和 (1,2)，那么 (2,3) 比 (0,2) 更可能"
"某些边的组合经常一起出现在优质解中"
"避免某些会导致 4-环的结构"
```

**特点**:
- ✅ 概率生成（探索多样性）
- ✅ 学习分布（发现新模式）
- ✅ 可控制（Temperature 调节随机性）

---

### 为什么迭代有效？

```
第 1 轮:
局部搜索 → 54 条边的解
↓
全局搜索 → 学习"54 条边的模式"
↓
采样 → 带有"54 条边基因"的种子

第 2 轮:
局部搜索 → 从"54 条边基因"出发 → 58 条边
↓
全局搜索 → 学习"58 条边的模式"（更优！）
↓
采样 → 带有"58 条边基因"的种子

第 3 轮:
局部搜索 → 从"58 条边基因"出发 → 61 条边
↓
全局搜索 → 学习"61 条边的模式"（更接近最优！）
...

第 77 轮：达到 63 条边（最优解）!
```

**关键**: 每一轮的全局搜索都为下一轮提供更好的起点

---

## 何时使用 PatternBoost？

### ✅ 适用场景

- 组合优化问题（图、集合、排列等）
- 有明确的约束条件
- 可以定义评分函数
- 解可以序列化表示
- 局部搜索可行

**典型问题**:
- 最大独立集
- 图着色问题
- 旅行商问题 (TSP)
- 背包问题
- 调度问题

### ❌ 不适用场景

- 连续优化问题（如函数最小化）
- 约束过于复杂（无法有效局部搜索）
- 解空间太小（无需迭代）
- 无法定义有效评分

---

## 快速实施指南

### 伪代码

```python
def pattern_boost(problem, n_epochs=100):
    database = []
    
    for epoch in range(n_epochs):
        # Phase 1: 局部搜索
        new_solutions = []
        for i in range(pop_size):
            if epoch == 0:
                seed = random_empty_graph()
            else:
                seed = sample_from_transformer(model)
            
            improved = local_search(seed, problem)
            new_solutions.append(improved)
        
        # Phase 2: 选择最优
        new_solutions.sort(key=lambda x: x.score, reverse=True)
        elite = new_solutions[:pop_size // 100]  # 前 1%
        
        # Phase 3: 训练 Transformer
        model = Transformer()
        train(model, tokenize(elite))
        
        # Phase 4: 记录最佳
        database.extend(new_solutions)
    
    return max(database, key=lambda x: x.score)
```

### 关键参数

```python
config = {
    "n_epochs": 100,           # 迭代轮数
    "pop_size": 100000,        # 种群大小
    "elite_ratio": 0.01,       # 使用前 1% 训练
    "temperature": 0.6,        # 采样温度（推荐）
    "top_k": 50,               # Top-K 采样
}
```

---

## 总结

### PatternBoost 的核心洞见

1. **没有银弹**: 单一方法总有缺陷
2. **优势互补**: 让每种方法做最擅长的事
3. **迭代增强**: 1+1 > 2 的关键是正反馈循环
4. **人机协作**: 不是替代人类，而是增强能力

### 为什么这个方法重要？

✅ **对数学**: AI 辅助发现新构造、打破猜想  
✅ **对 AI**: 打开黑盒，展示学习到的模式  
✅ **对科学**: AI for Science 的成功案例  

### 哲学意义

**最好的方法往往不是单一策略，而是看似对立的方法的辩证统一。**

- 保守（局部）vs 创新（全局）
- 确定性 vs 概率性
- 利用 vs 探索

**PatternBoost = 工匠精神 + 梦想家的创造力**

---

**深入阅读**: [`PATTERNBOOST_DEEP_DIVE.md`](PATTERNBOOST_DEEP_DIVE.md) - 完整详细版
